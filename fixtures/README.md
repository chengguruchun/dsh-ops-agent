# fixtures

运维 agent 会话剧本（建设中），例如：

1. Alertmanager 开火 → 拉 Grafana / 日志 → 定位服务 → 出缓解步骤  
2. 长排查过程触碰 context pressure，验证 `vetoWhileInvestigating` / `opsAgentPreservePlan`

先放纯文本剧本与假日志；接上 stock DSH 后再变成可执行脚本。
